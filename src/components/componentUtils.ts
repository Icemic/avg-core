import componentify from "./pixi/componentify";
import { mountNode, setValue, updateNode, updateValue } from "./pixi/properties";

const tools = { componentify, mountNode, updateNode, setValue, updateValue };

export default tools;
